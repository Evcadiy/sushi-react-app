import React from "react";

import styles from "./CartButton.module.scss";

const CartButton = () => {
  return <button className={styles["cart-button"]}>Cart</button>;
};

export default CartButton;
